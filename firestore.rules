rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All Firestore access goes through the Express backend using Firebase Admin SDK,
    // which bypasses these rules. Denying all client-side access here is intentional.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
